# ============================================
# InsightEngine Desktop - Environment Variables
# ============================================
# Copy this file to .env and fill in the values.
# On Windows: copy .env.example .env
# On Linux/macOS: cp .env.example .env

# --- App Mode ---
# "fake" = uses local JSON fixtures, no API keys needed (default)
# "real" = connects to real YouTube/Google APIs
APP_PROVIDER=fake

# --- Google OAuth (required for real mode) ---
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret

# --- Google Cloud (required for Vertex AI proxy) ---
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# --- LLM API Keys (optional, for real mode AI features) ---
OPENAI_API_KEY=
GEMINI_API_KEY=

# --- LLM Proxy Server Settings ---
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
LLM_RATE_LIMIT_PER_MINUTE=60
LLM_TIMEOUT_MS=30000

# --- Server configuration (optional) ---
API_BACKEND_PORT=5000
API_BACKEND_HOST=127.0.0.1
API_PAYLOAD_MAX_SIZE=7mb

# --- Recording (development only) ---
# Set to "1" to record real API responses as fixtures
RECORD_FIXTURES=0

# Before running the server, authenticate with:
#   gcloud auth application-default login
